const initialState = {
  lines: ['Hello this is a test', 'this is another line test', 'here is one more for good measure']
};

export const scenePartnerReducer = (state=initialState) => {
  return state;
};
